{
  "name": "@observatory/crawler",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The mailscape CLI. Thin orchestration over the packages.",
  "bin": {
    "observatory": "./dist/cli.js"
  },
  "main": "./dist/cli.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@observatory/core": "workspace:*",
    "@observatory/dns": "workspace:*",
    "@observatory/parsers": "workspace:*",
    "@observatory/store": "workspace:*",
    "bottleneck": "^2.19.5",
    "pino": "^9.6.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "pino-pretty": "^13.0.0"
  }
}
