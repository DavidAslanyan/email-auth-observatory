{
  "name": "@observatory/web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static adoption dashboard published to GitHub Pages.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --build && vite build",
    "preview": "vite preview",
    "clean": "tsc --build --clean && rm -rf dist"
  },
  "dependencies": {
    "@observatory/core": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "recharts": "^2.15.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.5"
  }
}
