{
  "name": "@observatory/dns",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Raw DNS resolution with four-state status classification and DoH fallback.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@observatory/core": "workspace:*",
    "bottleneck": "^2.19.5",
    "dns-packet": "^5.6.1"
  },
  "devDependencies": {
    "@types/dns-packet": "^5.6.5"
  }
}
